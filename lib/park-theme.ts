// /Users/avis/Desktop/orchid/lib/park-theme.ts

interface ParkTheme {
    primaryColor: string;
    secondaryColor: string;
    backgroundColor: string;
    fontFamily: string;
    fontSize: string;
}

const parkTheme: ParkTheme = {
    primaryColor: '#4CAF50', // Green
    secondaryColor: '#FFEB3B', // Yellow
    backgroundColor: '#FFFFFF', // White
    fontFamily: 'Arial, sans-serif',
    fontSize: '16px'
};

export default parkTheme;